# 🎲 '나의 번호' (My Numbers) 기능 개선 계획서

## 1. 개요

사용자가 '나의 번호' 페이지에서 번호를 선택할 때, 현재 선택 상태에 대한 명확한 피드백을 제공하여 사용성을 개선합니다.

## 2. 개선 사항

### 2.1. 실시간 선택 상태 안내 (UI/UX)

**"현재 3개 선택됨 (3개 더 선택하세요)"**

- **기능 정의**: 사용자가 번호를 클릭할 때마다 상단 안내 메시지를 실시간으로 업데이트합니다.
- **상태별 메시지**:
  - 0개 선택 시: "아래 번호를 클릭하여 6개를 선택해주세요."
  - 1~5개 선택 시: "현재 **N개** 선택됨 (**M개** 더 선택하세요)" (진행률 표시)
  - 6개 선택 완료 시: "🎉 6개 선택 완료! 아래 탭에서 분석 결과를 확인하세요."
  - 6개 초과 시도 시: "최대 6개까지만 선택할 수 있습니다." (Toast 메시지 유지)

- **시각적 피드백**:
  - 선택된 개수에 따라 진행률 바(Progress Bar) 또는 색상 텍스트로 직관적인 안내 제공.

## 3. 구현 계획

### 3.1. `src/web_app.py` 수정

- `my_number_page` 함수 내 번호 선택 로직 개선.
- `st.session_state.my_numbers` 길이를 확인하여 동적 메시지 생성.
- `st.progress` 또는 `st.info/warning/success` 컴포넌트 활용.

## 4. 기대 효과

- 사용자가 남은 선택 개수를 직관적으로 파악 가능.
- 번호 선택 과정의 지루함 감소 및 게임화 요소 강화.
- 오입력 방지 및 명확한 가이드 제공.
